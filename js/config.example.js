// If both files exist (config.js and config.example.js), the site uses config.js.
// Fill your values here.
window.CFG = {
  // Google Apps Script Web App endpoint (from Deploy > Manage deployments)
  ENDPOINT: "PASTE_YOUR_APPS_SCRIPT_WEBAPP_URL_HERE",
  // Your WhatsApp number in international format, e.g., "54911XXXXXXX"
  WHATSAPP_PHONE: "549XXXXXXXXX"
};
